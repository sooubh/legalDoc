
// This file is for type support only. No code needed here unless you want to add custom types for the API.

export interface MermaidRequestBody {
  prompt: string;
}

export interface MermaidResponseBody {
  mermaid: string;
}
